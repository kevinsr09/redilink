{
  "name": "@redilink/core",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.js",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && tsc",
    "build:tsup": "tsup ./index.ts --format esm,cjs --dts",
    "build:tsup2": "tsup ./**/*.ts --format cjs --dts --target=node10 --cjsInterop"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@libsql/client": "^0.6.0",
    "@prisma/adapter-libsql": "^5.14.0",
    "tsup": "^8.0.2"
  }
}
